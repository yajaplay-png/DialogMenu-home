# MyHomes fixed source

Source rebuilt from the uploaded MyHomes files. HomesDialogListener centralizes dialog replacement and uses a one-tick handoff after custom clicks to avoid the client close/open race.

Build with the same Paper API/version as the original project. No build metadata (pom.xml/gradle/plugin.yml) was included in the upload.
